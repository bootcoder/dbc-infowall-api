.row
  %h1 Users Index - App Index Temp
  - if flash[:session]
    .panel.callout
      = flash[:session]
  -if current_user
    .row
      %h1
        = current_user.username

.row
  = link_to 'New User', new_user_path
  •
  = link_to 'Login', login_path
  •
  = link_to 'Logout', logout_path


#content
